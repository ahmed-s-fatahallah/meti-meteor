{
  "name": "Meti Meteor project",
  "version": "1.0.0",
  "description": "mini game as an implementation on vanilla html, css and JavaScript",
  "default": "index.html",
  "legacy": {
    "engines": {
      "browsers": "> 0.5%, last 2 versions, not dead"
    }
  },
  "scripts": {
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build ./*.html --public-url ./",
    "deploy": "gh-pages -d dist",
    "build": "parcel build ./*.html --public-url ./",
    "lint": "npx eslint ./src/js"
  },
  "keywords": [
    "HTML",
    "CSS",
    "JavaScript"
  ],
  "author": "Ahmed Saeed",
  "license": "ISC",
  "devDependencies": {
    "@parcel/babel-preset-env": "^2.8.3",
    "eslint": "^8.35.0",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "core-js": "^3.28.0",
    "gh-pages": "^5.0.0"
  }
}
